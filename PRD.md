# **Product Requirement Document (PRD): Kernel**

**Version**: 1.5 (Final) **Language**: Traditional Chinese (繁體中文) **Status**: Planning

---

# **1. 產品概述 (Product Overview)**

## **1.1 產品願景 (Vision)**

**Kernel (內核)** 是一款專為高效能知識工作者設計的「全方位第二大腦 (Digital Second Brain)」。它旨在解決現代人資訊焦慮、執行力不足與生活缺乏反思的問題。透過整合 PARA 方法論、原子習慣 (Atomic Habits) 與量化自我 (Quantified Self) 概念，打造一個從「快速捕捉」到「深度執行」再到「系統回顧」的閉環系統。

## **1.2 目標用戶 (Target Audience)**

- **軟體工程師/開發者**: 需要管理多個 side projects、學習新技術棧、並保持深度工作狀態。
- **創意工作者/PM**: 需要捕捉大量靈感，並將其轉化為可執行的專案。
- **自我成長追求者**: 熱衷於數據化生活、習慣養成與日記反思。

## 1.3 產品特點及名詞

- 資源 (Resource) : 指單一筆的文章資料及參考連結等資訊，可同時關聯多個專案及領域。
- 專案 (Projects) : 用於管理單一明確目標或工作之任務(Task)，包含任務列表、專案進度等資訊
- 領域 (Areas) : 用於管理特定領域之內容、習慣 (Habit)。
- 封存 (Archive) : 在本產品內, 沒有實質意義上的檔案封存, 取而代之的是 資源 (Resource) 的狀態變更 `Archived`
- 日記 (journal) : 用於每日追蹤進行中之專案、習慣、指標(Metrics)
- 指標 (Metrics) : 由使用者定義「要被長期觀察的狀態」，並能在 Journal 中穩定地進行量測。

---

# **3. 功能詳細規格 (Detailed Functional Specs)**

## 3.0 全站畫面布局 (Layout)

---

## **3.1 會員系統 (Membership & Auth System)**

### **3.1.1 身份驗證 (Authentication)**

- **註冊/登入方式**:
    - **Social Login (OAuth 2.0) Only**: 僅支援 Google 與 GitHub 登入 (針對工程師客群優化)，不提供傳統帳號密碼註冊。
- **安全性**:
    - 強制執行 JWT (JSON Web Token) session 管理。
    - 敏感數據傳輸全加密 (SSL/TLS)。

### **3.1.2 訂閱會員制 (Subscription Tiers)**

- **Free**:
    - **30天全功能試用**: 註冊後享 30 天完整 PARA 功能與數據寫入權限。
    - **試用期滿後**: 轉為 **唯讀模式 (Read-Only)**，僅限瀏覽歷史資料，無法新增或編輯。
    - 無設備數量限制。
- **Pro**:
    - **無限期完整權限**: 解鎖持續寫入、編輯與新增資料的功能。
    - **無限歷史數據**: 存取所有過去的日記與數據分析。

---

## **3.2 收件匣 (Inbox)**

### **1. 功能說明**

- 快速捕捉 (全域快捷鍵偵測開啟，快速建立資源)。
- 資源快速分流 (Quick Dispatch) 至 PARA 系統。
- inbox內文章本質為資源 (Resource), 初始狀態為 “pending”

### **2. 頁面**

- **快速捕捉彈窗 (Quick Capture Modal)**
    - **說明**: 全域快捷鍵喚起的懸浮視窗。
    - **頁面結構**: 置中、背景模糊遮罩、單純的輸入框。
    - **元件**:
        - `Title`: 純文字標題 (必填，純文字欄位).
        - `Content`: Markdown 編輯區 (必填，支援基本 MD 語法, 可直接貼入剪貼簿之圖片, 編輯器頂部保留基礎功能列).
        - `tags` : 標籤欄位 (非必填, 輸入標籤後按空格鍵即選擇或創建新標籤，keyin過程中有自動完成功能)
        - `DispatchSearchbar` : 搜尋輸入框(非必填，若無選擇則資源狀態為 `pending`)，包含
            - 搜尋輸入框
                - placeholder : ‘請輸入專案或領域名稱’，輸入文字後即時更新RECENTLY USED內容 (若已經有選取項目，則置頂)
            - 選項清單
                - RECENTLY USED
                    - 顯示十筆更新日期最近的專案及領域名稱(排序:已選取項目>專案>領域)，可複選
                - 說明:
                    - 若用戶有選取專案/領域，則將該資源建立關聯 (linkedProjects、linkedAreas)，並將狀態(`Status`)變更為 `processed`
                    - 若用戶選擇Action ‘直接存入資源’，並將狀態(`Status`)變更為 `processed`
            - UI示意
                
                ```markdown
                ┌──────────────────────────┐
                │ 🔍 搜尋或輸入…           │
                ├──────────────────────────┤
                │ RECENTLY USED             │
                │ 專案：半程馬拉松           │
                │ 專案：網站改版             │
                │ 領域：跑步                │
                │ 領域：健康                │
                │ …（最多 10 筆）            │
                └───────────────────────────┘
                ```
                
        - `sourceLink` : 參考連結
        - `Save Button`: 儲存按鈕。
        - `Canel Button`: 取消按鈕。
    - 資料:
        
        ```markdown
        Resource {
          id: Identifier
          title: string
          content: Markdown
          status: 'pending' | 'processed' | 'archived'
          linkedProjects?: ProjectID[]
          linkedAreas?: AreaID[]
          sourceLink?: URL
          tags: Tag[]
          createdAt: DateTime
          updatedAt: DateTime
        }
        ```
        
    - **行為**:
        - 按下 `Ctrl+Q` 喚起。
        - 輸入對應內容後 -> 按 儲存送出 -> 顯示成功提示並關閉。
        - 按 Esc 或取消按鈕關閉。
- **收件匣清單頁 (Inbox List Page)**
    - **說明**: 檢視並處理未分類事項列表 (狀態: `pending`)的頁面。
    - **頁面結構**: 列表呈現，頂部有操作工具列。
    - **頂部元件(header)**:
        - `search bar`: 搜尋/篩選列表資源
        - `create resource` : 新增一筆資源(狀態待處理)
    - 列表元件(body):
        - `title`: 標題，點擊後進入**收件匣內容頁**。
        - `createdAt`: 建立時間
        (時間格式範例 “12/30/25-16:05”)。
        - `Short Content` : 顯示內文前十個字元
        - `tags` : 顯示前三個tag (範例 : #運動#馬拉松#有氧), 總長度超過十個字之後則以”…”顯示
        - `Action icons`: 右側顯示 歸檔(dispatch)及移除(delete)按鈕
    - **行為**:
        - 清空後顯示 "Inbox Zero" 獎勵畫面。
        - 點擊歸檔按鈕則出現 Inbox Processing Modal
        - 點擊移除按鈕則刪除該筆資料 (需先彈出確認視窗)
- Inbox Processing Modal
    - 說明 : inbox中所選資料轉移至目標'領域’/’專案’/’資源’
    - **頁面結構**: 置中、背景模糊遮罩
        
        ```markdown
        ┌──────────────────────────┐
        │ 🔍 搜尋或輸入…           │
        ├──────────────────────────┤
        │ ACTION                    │
        │ • 直接存入資源             │
        ├──────────────────────────┤
        │ RECENTLY USED             │
        │ 專案：半程馬拉松           │
        │ 專案：網站改版             │
        │ 領域：跑步                │
        │ 領域：健康                │           
        │ …（最多 10 筆）            │
        ├──────────────────────────┤
        │ Cancel         ⏎ Confirm │
        └───────────────────────────┘
        ```
        
    - 元件:
        - 搜尋輸入框
            - placeholder : ‘請輸入專案或領域名稱’，輸入文字後即時更新RECENTLY USED內容 (若已經有選取項目，則置頂)
        - 選項清單
            - ACTION
                - 直接存入資源 (將此資源狀態改為:`processed`)
            - RECENTLY USED
                - 顯示十筆更新日期最近的專案及領域名稱(排序:已選取項目>專案>領域)，可複選
        - Action item :
            - 取消 : 關閉此彈出視窗
            - 確認 :
                - 若用戶有選取專案/領域，則將該資源建立關聯 (linkedProjects、linkedAreas)，並將狀態(`Status`)變更為 `processed`
                - 若用戶選擇Action ‘直接存入資源’，並將狀態(`Status`)變更為 `processed`
- **收件匣內容頁 (Inbox Content Page)**

---

## **3.3 專案管理 (Projects)**

### **功能特點**

- 目標導向 (Goal-Oriented) 設計。
- 看板 (Kanban) 與清單 (List) 視圖切換。

### **頁面**

- **專案列表頁 (Projects Index)**
    - **說明**: 總覽所有活躍與歸檔專案。
    - **頁面結構**: 卡片網格排列，區分 Active / Hold / Completed。
    - **行為**: 點擊卡片進入內頁。
    - **欄位**:
        - `Project Name`: 專案名稱。
        - `Status`: 狀態標籤。
        - `Progress`: 進度條 (%)。
        - `Deadline`: 截止日期。
- **專案詳情頁 (Project Detail)**
    - **說明**: 單一專案的執行戰情室。
    - **頁面結構**:
        - Top: 目標摘要、Deadline 倒數。
        - Main: 任務管理區 (Kanban/List)。
        - Side: 關聯資源與筆記。
    - **行為**:
        - 拖曳任務卡片改變狀態 (To Do -> Doing)。
        - 新增任務時自動帶入 `project_id`。
    - **欄位**:
        - `Goal`: 核心目標描述。
        - `Tasks`: 關聯的任務清單。
        - `Notes`: 專案專屬筆記。
        - `Area Link`: 所屬領域連結。

---

## **3.4 領域維護 (Areas)**

### **功能特點**

- 無截止日期的長期維護。
- 習慣管理配置中心 (Habit Config)。

### **頁面**

- **領域詳情頁 (Area Detail)**
    - **說明**: 管理特定生活面向 (如健康、財務) 的儀表板。
    - **頁面結構**:
        - Top: 願景/原則聲明。
        - Section 1: 習慣設定管理 (Habit Manager)。
        - Section 2: 活躍專案列表。
        - Section 3: 週期性任務。
    - **行為**:
        - 新增習慣:在此定義習慣 (e.g. "閱讀") -> 連動至日記頁面顯示。
        - 設定 KPI: 更新當前指標數值。
    - **欄位**:
        - `Principle`: 原則說明文字。
        - `KPIs`: 指標名稱與數值。
        - `Configured Habits`: 定義的習慣項目 (Name, Frequency)。
        - `Recurring Tasks`: 週期任務設定 (Cron expression)。

---

## **3.5 資源庫 (Resources)**

### **功能特點**

- 樹狀結構知識庫。
- 網頁內容剪藏 (Web Highlight)。

### **頁面**

- **資源列表頁 (Resource Explorer)**
    - **說明**: 瀏覽所有收藏內容。
    - **頁面結構**: 左側樹狀目錄 (Tree)，右側內容清單 (Grid/List)。
    - **行為**:
        - 點擊資料夾展開/收合。
        - 拖曳檔案移動資料夾。
    - **欄位**:
        - `Title`: 標題。
        - `Tags`: 標籤陣列。
        - `Type`: Note, Link, Image, PDF.
        - `URL`: 原始連結 (如為網頁)。

---

## 3.6 指標管理 (Metrics)

---

## **3.7 日記 (Journal)**

### **功能特點**

- 量化自我 (Quantified Self) 儀表板。
- 所見即所得 Markdown 編輯器，支援剪貼簿圖片上傳。
- 習慣與待辦的聚合顯示。

### **頁面**

- **每日詳情頁 (Daily Detail View)**
    - **說明**: 每一天的核心工作與反思介面。
    - **頁面結構**:
        - Header: 日期標題、連續天數統計。
        - Left: 月曆小工具 (Calendar Widget)。
        - Center: 編輯器 (Editor)。
        - Right: 側邊欄 (Inspector Panel)。
    - **行為**:
        - **編輯**: 輸入 Markdown，`Ctrl+V` 貼上截圖 -> 自動上傳至 Supabase Storage 並插入連結。
        - **評分**: 拖曳心情/能量滑桿 -> 數值更新。
        - **打卡**: 點擊習慣標籤 -> 標記完成 (變色)。
    - **欄位**:
        - `Date`: 該頁面日期 (Key)。
        - `Mood Score`: 1-10 整數。
        - `Energy Score`: 1-10 整數。
        - `Habit Log`: JSON (習慣ID + 完成狀態)。
        - `Content`: Markdown 字串 (日記本體)。
        - `Daily Tasks`: 關聯至此日期的 Task 列表 (唯讀/勾選)。

---

## **~~3.8 全域設定 (Settings) (暫不執行)~~**

### **功能特點**

- 高度客製化：讓使用者依據習慣調整操作手感。
- 即時預覽：外觀設定修改後立即生效。

### **頁面佈局 (Settings Layout)**

- **說明**: 全站統一的設定介面。
- **頁面結構**: 雙欄式佈局。
    - **Left (Sidebar)**: 導航選單，包含
        - 「帳號 (Account)」
        - 「系統 (System)」
        - 「外觀 (Appearance)」
        - 「快捷鍵 (Shortcuts)」。
    - **Right (Content)**: 對應的設定內容表單。

### **頁面 (Content)**

- **帳號 (Account)**:
    - 說明:以表單頁面陳列顯示資料
    - 元件/欄位
        - 帳號 (Account): 只顯示帳號無法變更
        - 頭像 (`Avatar`) : 可編輯 (上傳本地圖片)
        - 暱稱 (`Nickname`) : 可編輯
        - 儲存按鈕 (`Save button`)
        - 取消按鈕 (`Canel button`)
    - 行為
        - 用戶變更/編輯表單內容後顯示儲存按鈕&取消按鈕
        - 點擊儲存按鈕則送出變更
        - 點擊取消按鈕則恢復原始內容
- **系統 (System)**:
    
    暫無功能，此頁顯示 “Coming soon”內文
    
- **外觀 (Appearance)**:
    - `Theme Preset`: 提供 5 種預設風格 (Kernel Default, Professional, Hacker, Serene, Neon)。
- **快捷鍵 (Shortcuts)**:
    - 說明 : 陳列動作名稱對應按鍵組合。點擊欄位可錄製新按鍵。
    - 快捷鍵功能及預設按鍵
        - 打開收件匣快速捕捉視窗  (Zero Friction Capture) : Ctrl + Q

# **4. 系統技術架構 (Tech Stack)**

- **Frontend**: Next.js 14 (App Router), TailwindCSS, **shadcn/ui**.
- **Editor**: **Plate.js** (基於 Slate.js，提供 Notion-like 體驗與強大的 Plugin 生態)。
- **Backend**: **Supabase** (PostgreSQL, Auth, Storage, Realtime).
- **State Management**: Zustand (UI) + React Query (Data).
- **Deployment**: Vercel.

# 5. 設計風格 (Design Guide)

# 6. 開發階段 (Roadmap)

## 6.1 Phase 1 (Core)

## 6.2 Phase 2 (Prototype)

## 6.3 Phase 3 (Function)

## 6.4 Phase 4 (Secure)

## 6.5 Phase 5 (Prod)